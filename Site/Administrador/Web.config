<?xml version="1.0"?>
<configuration>
    <system.web>

      <!-- Regra de Acesso para o conteudo da pasta /Admin -->
      <authorization>
        <!-- Regra: Proibir Usuarios Anônimos [?] -->
        <deny users="?"/>
      </authorization>

    </system.web>
</configuration>
